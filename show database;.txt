show database;

use db_lesson;

Q1
CREATE TABLE departments (
    -> department_id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    -> name VARCHAR(20) NOT NULL,
    -> created_at TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
    -> updated_at TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
    -> );

Q2
alter table people add department_id int unsigned null after email;

Q3
insert into departments (name)
    -> values
    -> ('営業'),
    -> ('開発'),
    -> ('経理'),
    -> ('人事'),
    -> ('情報システム');

 insert into people (name, department_id, age, gender)
    -> values
    -> ('大野智',1,44,1),
    -> ('櫻井翔',4,43,1),
    -> ('相場雅紀',1,42,1),
    -> ('二宮和也',5,41,1),
    -> ('松本潤',2,41,1),
    -> ('百田夏菜子',1,30,2),
    -> ('玉井詩織',3,29,2),
    -> ('有安杏果',2,30,2),
    -> ('佐々木彩夏',2,28,2),
    -> ('高城れに',2,31,2);

insert into reports (person_id, content)
    -> values
    -> (7, '今日はりんごをたべました。'),
    -> (8, '今日はなしをたべました。'),
    -> (9, '今日はすももをたべました。'),
    -> (10, '今日はいちごをたべました。'),
    -> (11, '今日はオレンジをたべました。'),
    -> (12, '今日はチョコレートケーキをたべました。'),
    -> (13, '今日はアイスクリームをたべました。'),
    -> (14, '今日はピクルスをたべました。'),
    -> (15, '今日はチーズをたべました。'),
    -> (16, '今日はサラミをたべました。');

Q4
update people set department_id = 1 where department_id is null;

Q5
SELECT age, name FROM people where gender = 1 order by age DESC;

Q6
peopleテーブルのnameカラム、emailカラム、ageカラムを
department_idレコードが1の人のみ、
作成時刻の昇順で並び替え、取得する。

Q7
SELECT name FROM people where (age between 40 and 49 and gender = 1) or (age between 20 and 29 and gender = 2);

Q8
SELECT * from people where department_id = 1 order by age ASC;

Q9
SELECT AVG (age) AS average_age from people where department_id = 2 and gender = 2;

Q10
SELECT p.name, p.department_id, r.content from people p inner join reports r using (person_id);

Q11
SELECT p.name from people p left outer join reports r using (person_id) where r.content is null;